# Glitch Survivors — 変更ログ

このゲームの更新履歴。メニュー画面の「📜 変更ログ」と同じ内容を、リポジトリにも残す。
バックアップは `backups/<日付>-<連番>-before|after/` に、デプロイ時のスナップショットは `deployed/<日付>/` に保存している。

## Build 63 — 2026/06/26
- 🎮 **カメラ基準の移動に修正**：入力の回転を `+camYaw`→`-camYaw`（`wx=ix*cos+iz*sin, wz=-ix*sin+iz*cos`）。カメラの周回方向と入力回転が逆だったため回転後に前後反転していた。デフォルト(camYaw=π)は不変、回転時のみ正される
- 📱 **モバイルのカメラ回転を追加**：touchハンドラを multi-touch 対応に。1本目=ジョイスティック(移動)、2本目=`camTouch` でドラッグ→camYaw/camPitch 回転。ピンチズームは廃止（2本目を回転に割当）
- 📈 XPカーブの倍率 ×3→×5：`GEO5=[_,5,55,95,145,210,295,410,560,760,1025]`、Lv10以降 `1025*1.5^floor((L-10)/5)`

## Build 62 — 2026/06/26
- 🧪 テストモードの **2キーを「強化チケット+13」** に変更（`player.credits+=13`＋updateShopBtn）。3キー=3:00ジャンプは据え置き。KeyTのトーストも更新
- 📈 XPカーブ再設計：`GEO3=[_,5,33,57,87,126,177,246,336,456,615]`（元1.32カーブ×3、L1→2=5据え置き）を Lv10 まで使用、**Lv10以降は `615*1.5^floor((L-10)/5)`**（5レベルごとに×1.5の階段）。レベルが速すぎる問題を調整。例: Lv5→126 / Lv9→456 / Lv10〜14→615 / Lv15〜19→923 / Lv20〜24→1384 / Lv25〜29→2076

## Build 61 — 2026/06/26
- 📈 XPカーブを**元の幾何級数（×1.32+4）に戻し、Lv13以降はフラット**：`consumeLevel` を `Math.min(488, Math.round(player.xpNext*1.32+4))`。Lv13→14 で 488 に達したあとは全レベル 488 で一定（元は 648→854→… と爆発）。中盤は元の手応え、終盤はカードが取り続けられる。初期 xpNext=5 据え置き

## Build 60 — 2026/06/26
- 📈 XPカーブを**幾何級数→レベル基準のべき乗**に変更：`consumeLevel` の `xpNext*1.18+4` → `Math.round(5.7*Math.pow(player.level,1.32))`。加速し続ける形をやめ「中盤やや難・終盤フラットで易」に（中盤 Lv8 65→89・Lv10 100→120／終盤 Lv15 258→203・Lv20 618→300・Lv25 ~1400→402）。初期 xpNext=5 据え置き。XPバーのみ変更（敵性能は不変）

## Build 59 — 2026/06/26
- 📈 **レベルアップ必要XPの伸びを緩やかに**：`consumeLevel` の `xpNext*1.32+4` → `xpNext*1.18+4`。後半の指数的高騰を抑え、カード入手を維持しやすく（Lv20の必要XP 約2600→約500）。初期 xpNext=5 は据え置き

## Build 58 — 2026/06/26
- ⚔️ **3:00 以降に湧く敵は HP×2・XP×2**。spawnSlime に `lateMul=(!boss && game.time>=180)?2:1` を追加し hp/maxhp/xp に乗算（既存の連続 enemyHpScale に上乗せ。bossは対象外）。難度UP＋レベルアップ加速

## Build 57 — 2026/06/26
- 🧪 **テストモード**：keydown に `KeyT` トグル（`game.testMode`）、ON中は `Digit3`→`game.time=180`、`Digit2`→`game.time=120`（playing時のみ）。後半スポーンを待たず検証可。testModeはresetGameで消えず継続
- 🌟 新敵の**同時出現上限 8→20**（spawnRing の mc 判定）。0.80 ロールは効いていたが上限8で頭打ち＝体感2割だったため引き上げ。※モバイルで重ければ要調整

## Build 56 — 2026/06/25
- 🐛 **新モデル敵（bat/dragon/skeleton/slime）が完全透明で描画されない不具合を修正**。原因＝FBX→glb 変換でマテリアルの baseColor alpha=0（`material.opacity:0`）が焼き込まれていた（メッシュ・スキン・座標は正常、opacityだけ0で不可視）。CINC+`window.__rpg` で実機検証→opacity=1 で即描画を確認。`opaqueClone()` を追加し instApple/instBear/instModel の全マテリアルを `transparent:false / opacity:1 / depthWrite:true` でクローン（Hexabear/apple にも予防適用）

## Build 55 — 2026/06/25
- 🌟 3:00 以降の新敵(bat/dragon/skeleton/slime)の**出現率 0.22→0.80**（`pickType` の t>=180 分岐）＝3分以降は大半がこの4体
- ⬆️ この4体の**XPを3倍**（bat 4→12 / dragon 30→90 / skeleton 7→21 / slime 5→15）でレベルアップ高速化。spawnRing の同時上限8は維持

## Build 54 — 2026/06/25
- 🌟 **3:00 経過後に Quaternius の CC0 アニメ付きモンスター4体が通常スポーンに混ざって出現**：🦇bat（速い飛行）/🐉dragon（強い大型・飛行）/💀skeleton（走る）/🟢slime。Drive(CC0 Animated Monster Pack) の FBX を Blender 5.1.2 で glb 変換（`assets/{bat,dragon,skeleton,slime}.glb`・各5前後のアニメ込み）
- 実装: `MODELS` レジストリ＋`instModel()`（SkeletonUtils.clone＋AnimationMixerで移動クリップ Flying/Running/Walk を再生）。`pickType` に `t>=180` で約22%これらを返す分岐（dragon は低確率）。`buildEnemyMesh` に `MODELS[typeKey]` 分岐（未読込時はslimeにフォールバック）。アニメ更新は enemy ループの `e.mixer` 分岐（飛行=ホバー+bob、地上=小bob、被弾=emissive赤フラッシュ）
- ETYPES に4種追加（bat/dragon/skeleton/slime・hp/speed/dmg/scale/xp）。**perf: 同時モデル敵≤8**（`spawnRing` で超過分は grunt に置換）。net 同期は ETYPE_KEYS 追記でguestも生成
- ※モデルの向き/サイズ/ホバー高さは実機確認前提（ヘッドレス検証不可）。nscale(1/実寸高さ)で正規化→archetype scale=最終高さ

## Build 53 — 2026/06/25
- 📱 **「外に出る」ボタンを2回タップすると画面がズーム/崩れする不具合を修正**（build 52で取り切れなかったケース）。原因＝1回目のタップで `closeShop()` が走り `#levelup` が `display:none` になるため、ダブルタップの**2回目が地面/canvas に当たり**、UI限定だった build 52 のガードをすり抜けて iOS のダブルタップ拡大が発火していた。iOS は「同一座標への素早い2連タップ」だけで拡大判定するので、ガードを **`document` 全体（グローバル）に拡張**。同一座標(±40px)・350ms以内の2連タップだけ `preventDefault()`（`e.cancelable` 時のみ）。単発タップ・別座標の連続タップ・canvas のジョイスティック（touchstart/move 直処理）は不変
- ※ build 52 は `#levelup,#overlay,…` に `closest()` で限定していたのが穴だった

## Build 51 — 2026/06/25
- 🍎 回復アイテム（リンゴ）を**実3Dモデル `assets/apple.glb`** に差し替え（石井さん作の `apple_final.blend` を Blender 5.1.2 ヘッドレスで GLB 変換）。`dropHeart` で `instApple()`（読込済なら clone、未読込なら赤球フォールバック）。scale 0.085・y-4cm 調整
- 🐻 巨大グマ（Lv6）を**CC0 モデル `assets/bear.glb`（Hexabear / Polygonal Mind・CC0）** に差し替え。`buildEnemyMesh` の bear6 で `instBear()`（SkeletonUtils.clone・skinned）、未読込/失敗時は手続き熊にフォールバック。ブルート(Lv5)は手続き熊のまま
- glTF 読込基盤 `GLB`＋ローダー追加。モデル熊はアニメ無しのため「全体の上下動＋被弾時 emissive 赤フラッシュ」で `modelBear` 分岐。`assets/` は `.vercelignore` 対象外＝Vercel が配信
- ※モデルの向き/サイズは実機確認前提（ヘッドレス検証不可）。ズレたら scale(0.085 / bear archetype 2.16)・回転・y を微調整

## Build 50 — 2026/06/25
- 🐻 新アーキタイプ **`bear6`（巨大グマ／Lv6）** 追加：`ETYPES` に hp340/speed2.2/dmg22/scale2.16(ghost×2)/xp40。`player.level>=18` で `game.bear6T` タイマー駆動・約40秒ごとに1体接近スポーン。`noKnock` フラグで**ノックバック無効**（hurtEnemy の knock 適用を `!e.noKnock` でゲート）。procedural bear（濃色）使用、ETYPE_KEYS に追加で net 同期も対応
- 🐻 熊のアニメ修正：胴体だけ squash する slime 用モーションをやめ、`bearParts`(脚/腕)を持つ敵は**脚・腕を振る歩行＋穏やかな上下動**に分岐。ブルート(Lv5)/巨大グマ(Lv6)両方に適用
- ※「本格的な熊」=CC0 実3Dモデル(.glb)の差し込みは次ビルド予定（現状は手続き生成）

## Build 49 — 2026/06/25
- 🎯 バトルモードの**射程を約半分**に：弾の寿命 2.6→1.3秒・距離キャップ 70→35（`mpBattle()` でゲート、ソロ/協力は2.6/70のまま）。射程は「永遠」ではなく元々 寿命×速度24 ≈ 62 で消えていた
- 🐻 brute（赤・大型・低速）の見た目を**手続き生成のローポリ熊**に差し替え（`buildBear`／`buildEnemyMesh`）。胴体=`e.body` でダメージ点滅・潰れアニメ・撃破バーストは従来通り。solo（spawnSlime）と協力guest（mpMakeNetEnemy）の両方で熊化。性能（HP/攻撃/速度）は不変

## Build 48 — 2026/06/25
- 🛡️ アーマー（パッシブ）を固定値 -2/Lv → **割合カットに変更**。各Lv -14%、max5 で **-70%カット**（`player.armor` を 0〜0.70 の係数化、hurtPlayer: `n*(1-armor)`、PvP takePvpDamage: `d*0.7*(1-armor)`）。文字も「被ダメージ -14%（最大-70%）」に

## Build 47 — 2026/06/25
- 🚪 ショップ（家）から「外に出る」と、現在地から14〜24離れた**フィールドのランダム安全地点に移動＋約0.8秒の無敵**（randomFieldSpot：場外・障害物を回避）。出口での待ち伏せ対策。全モード
- 👥 協力/対戦で**相手が一瞬消える現象を軽減**：相手データが単発で null（通信の瞬断）になっても即非表示にせず、2.5秒データが来ない時だけ退出扱い（mpOnRemote/mpTick の de-bounce、MP._remoteGoneAt）

## Build 46 — 2026/06/25
- 🔧 ボルト自動成長が**ロードアウトのレベル表示に即反映**されるよう修正（addXP のレベルアップ処理に renderLoadout('bolt') が漏れていた）。同じく checkEvolutions() を追加し、自動成長でMAX(8)到達時の**ストームボルト進化**も発火するように

## Build 45 — 2026/06/25
- ✨ マジックボルトの**自動レベル上げを全モードに展開**（バトル専用→協力・ソロでもレベルアップで自動 +1、max8）。自動成長するためショップのボルトカードは全モードで廃止

## Build 44 — 2026/06/25
- ⚔️ バトルのアーマー（PvP被ダメ軽減）を 60%→**30%カット**に変更（takePvpDamage: ×0.4 → ×0.7）。アーマー passive の flat -armor は据え置き

## Build 43 — 2026/06/25（対戦モード調整）
- ⚔️ バトル：マジックボルトをショップから除外し、レベルアップで自動強化（max8）
- ⚔️ PvP被ダメージ ~60%軽減（takePvpDamage で ×0.4）＝近距離が即死しにくく
- ⚔️ 基礎体力3倍：battleSpawn で maxhp/hp=300
- ⚔️ オービットガード/フレイムオーラはレベル1の敵(swarm, ki=3)を一撃（9999ダメージ）。対プレイヤー(pvpHurt)は通常dpsのまま
- 💎 経験値結晶のサイズも価値で変化（shardScale: v1=0.8 → v12=2.0, ボス1.7）＝色＋大きさで価値が一目で分かる（全モード）
- 協力・ソロは戦闘調整なし（mpBattle ガード）

## Build 42 — 2026/06/25
- 🛒 ショップに「🔄 選び直す」ボタン：開いた時点のスナップショット（クレジット/武器/パッシブ/進化/各ステータス/HP）を保持し、押すとその回の購入を全て払い戻して選び直せる。1クレジットで買い切って詰む問題を解消。購入後のみ表示

## Build 41 — 2026/06/25
- 💎 経験値ジェムの色を価値で変化：v1淡青 / v2シアン / v3青 / v5紫 / v12琥珀 / ボス金。`shardColor(value)` で自動決定、`dropShard` のデフォルト色をvalue連動に

## Build 40 — 2026/06/25
- 🔊 発射音を `shoot.m4a` に差し替え
- 📱 iOS ダブルタップズーム無効化：操作UIに `touch-action:manipulation`（スクロールは維持）
- ⚔️ バトル開始位置を左右に（p1=−X / p2=+X、互いに向き合う）。リマッチも同様
- 🌳 敵↔障害物(家/木/岩)の当たり判定を追加（host/solo権威で押し出し）。弾も障害物で消滅（壁を貫通しない）
- 協力プレイの進行・開始位置は据え置き（中央スタートのまま）

## Build 39 — 2026/06/25
- 🍎 回復ハートのドロップ確率を半減（通常敵 4%→2%）。ボスの確定ドロップは据え置き
- 👾 2:00(120s)以降の敵抽選を変更：レベル3+(grunt/ghost/brute)を約90%、レベル1-2(swarm/runner)を約10%（強さ＝swarm1/runner2/grunt3/ghost4/brute5）

## Build 38 — 2026/06/25
- 🔊 発射音(sfx.shoot)を差し替え：`shoot.mp3`（パンチの効いたレーザー）をWebAudioでデコードし、ショットごとにbuffer sourceで再生（重複OK）。master gain経由なのでミュートも効く。デコード完了までは従来のビープにフォールバック

## Build 37 — 2026/06/25
- ✨ マジックボルトを狙い撃ち（向いた方向へ連射）に再設定。fire() が毎回プレイヤーの向きから方向を算出するため、**レベル・進化・マルチショットに関係なく狙いは維持**（=「アップグレードで狙いが消える」バグなし）。見た目は build 33 のまま、他武器は自動ターゲット

## Build 36 — 2026/06/25（巻き戻し）
- ↩️ アート（世界観）を **build 33 の見た目に戻した**（build 34 のほのぼのローポリ・ファンタジー化を撤回）
- これに伴い build 35 のマジックボルト狙い撃ちも一旦消去（build 33 時点の自動ホーミングに戻る）。必要なら再追加可能
- 中身は build 33 の index.html を復元し、配信のため BUILD だけ 36 に採番

## Build 35 — 2026/06/25
- ✨ マジックボルト（デフォルト武器）を**狙い撃ち**式に：自動ホーミングをやめ、プレイヤーの向き（最後の移動方向）へ連続発射。発射レートも上げて連射感に。他の武器は自動ターゲットのまま

## Build 34 — 2026/06/25（アート方向：ほのぼのローポリ・ファンタジー フェーズ1）
- 空・フォグ・ライトを暖色の昼に（HemisphereLight/Sunを暖色化）
- 敵をパステル＆発光控えめに（saturation/emissive/lum調整）
- FXを暖色の魔法系に：弾＝金、オーブ＝若草、XP＝金、ノヴァ/着弾＝金
- クリスタルを琥珀/金の妖精結晶に、草を少し暖色に、きのこ36個を追加
- ※名前・主人公モデルは据え置き（フェーズ2でモデル差し替え・UIリテーマ予定）

## Build 33 — 2026/06/24
- 🛒 iOS Safari でショップが潰れて重なる不具合を修正：開いた瞬間に強制リフロー（display トグル＋offsetHeight）＋ `#levelup` に実ビューポート高を明示。触らなくても初回から正常
- die/reset/battleEnd でも明示高さをクリア

## Build 32 — 2026/06/24
- 🛒 スマホ縦画面でショップのカードが潰れて重なる不具合を修正（触らなくても2枚目の正常表示になる）
- カード一覧を flex 内部スクロール化（`vh` 依存を撤廃・`min-height:0`・カード最小高96px）。初回描画から正しい高さ

## Build 31 — 2026/06/24
- 📱 スマホ縦画面の最初の画面（メニュー）の崩れを修正：「スクロールしないと直らない」問題を解消
- ビューポート確定後（load/orientationchange/visualViewport/遅延）にキャンバスを再フィット
- メニューカードを縦画面サイズに縮小＋短い画面ではスクロール可（クリップ防止）

## Build 30 — 2026/06/24
- ⚔️ **バトル（対戦）モード**追加：メニューに「バトル」。バトル同士で自動マッチ（協力とは別ロビー）
- 敵も湧くカオス対戦。自動武器（弾・ノヴァ・メテオ・オーブ・炎・ブレード・スパーク）が**相手にも命中**
- **3回やられたら負け**（ライフ3）。やられたら離れた所に復活＋2.5秒無敵（点滅）
- 相手のライフ＆HPを画面上部に表示。決着でリブート→同期リマッチ
- PvPダメージは書き込み可能な `state/pvp_*`・`state/lives_*` 経由＝Firebaseルール変更なし
- 家（ショップ）に入っている間はPvP無敵（安全に強化）

## Build 29 — 2026/06/24（最適化）
- ⚡ 全モード軽量化：タッチ端末はピクセル比1.5＋アンチエイリアス無効（retinaで大幅高速化）
- 影マップ 2048→1024（4倍軽量）、敵・オーブ・XPの影を無効化（混戦で効く）
- 敵150体・弾140発・XP160個の上限を設定（暴走防止）。同時ダメージ数字も24までに
- マルチも同じ最適化が効く（敵上限でホスト配信/シミュレーションも軽量化）

## Build 28 — 2026/06/24
- 🛒 **ショップ制に変更**：レベルアップは自動で3枚から選ぶのをやめ、**強化チケット**が1枚貯まる（メーター=レベル）
- 🛒ボタン（画面下中央／B キー）でショップ＝家に入る。**全カード**（武器8＋パッシブ12）が並び、**チケット1枚で1カード**購入
- MAX/進化済みは購入不可表示。購入のたびクレジット消費・即反映
- ショップ＝家なので MP では安全・不可視・相手に敵が向く（既存の家ロジックを流用）

## Build 24〜27 — 2026/06/24
- 🏠 **レベルアップ＝安全な家（シェルター）に入る**方式に統一（build 22 の「逃げながら選ぶ」を置き換え）
- 家の中は**敵の標的から外れる**＋**接触ダメージ無敵**（移動・武器は停止）
- MP：一人が家に入ると**敵がもう一人（外にいる相手）を標的**にする
- MP：家の中は**相手から見えない**（アバター・名札・攻撃を非表示／煙パフ演出）
- 内装は家テーマのオーバーレイ（木の室内）。GPT生成の内装イラスト `house-interior.png` を全画面背景に採用（build 27）
- 📜 変更ログを**常時**開ける右上ボタンを追加
- 同期は書き込み可能な `state/house_{slot}` 経由＝**Firebaseルール変更なし**（接続を壊さない）
- 在宅中は家フラグを2秒ごとに再送（鮮度維持）。フリーズ時は5秒で自動的に「外」に戻し相手を再表示（build 25）
- 在宅判定を受信側の到着時刻ベースに（端末間の時計ズレに影響されない／実機2台で正しく動く）（build 26）

## Build 23 — 2026/06/24
- 仲間（相方）の見た目を**通常の色**に戻した（幽霊っぽい青の着色を廃止）
- 各プレイヤーの頭上に**プレイヤータグ**を表示（自分=金「プレイヤーN」/ 相方=水色）
- マルチの**リスタートを同期**：どちらかが「再挑戦」を押すと**両方が同時に**新しいゲームへ
- メニューに**変更ログ**パネルを追加（この履歴を表示）

## Build 22 — 2026/06/24
- 経験値を敵の強さで変化（群れ1 → 標準3 → ゴースト5 → 重装甲12 → ボス55）
- 敵タイプごとにスピードのメリハリを強化（俊足を速く・重装甲を遅く 等）
- 自分の攻撃力を全体 +20%
- マルチのレベルアップは**一時停止せず**画面上部にカード表示（逃げながら選択 / PC は J・K・L キー）。シングルは従来どおり中央＋一時停止

## Build 21 — 2026/06/24
- co-op：仲間の攻撃（弾・周回オーブ・炎オーラ）が見えるように同期（書き込み可能な `state/` 経由、ルール変更なし）

## Build 18〜20 ＋ オンライン修正 — 2026/06/24
- **オンライン接続の根本修正**：プレイヤー位置情報（`down`/`rp` フィールド）が古い Firebase ルールで弾かれ、ホストが登録できず「全く繋がらない」状態だった。ルールに該当フィールドを許可してデプロイ → 接続が回復
- マッチメイキング：幽霊部屋（生存ホストがいない部屋）に入らず、新しいホストになる。同時タップの二重ホスト競合も回避
- 敵同期の安定化：両プレイヤー周辺の敵を配信／鮮度タイムスタンプ＋猶予でちらつき防止／ホスト消失からの自動再マッチ

## Build 14〜17 — 2026/06/24
- 自動アップデート：端末が自動で最新ビルドに更新（キャッシュ起因の「更新されない」を解消）
- co-op の開始を堅牢化：永続「開始」フラグでスリープ中でも確実に同時スタート、ハートビートの安全網
- ライブ接続バッジを表示（ホスト＝配信中の敵数 / ゲスト＝受信した敵数）
- オービット／炎の継続ダメージで出る「0」表示を抑制

## 2026/06/23 — 基盤
- オンライン マルチプレイ追加（部屋コード無しの自動マッチメイク・リアルタイム同期）
- ホスト権威の敵同期（2人で同じ敵の群れを共有）
- 武器進化システム ＋ 武器4種・パッシブ3種を追加、レゾナンス強化
- co-op 蘇生（ダウン → 仲間が近づいて救助）＋ BGM
- 敵5種類 ＋ 全方位からの群れ ＋ フローティングジョイスティック（ダッシュボタン廃止）
- メインメニュー（1人 / 2人）＋ 同時スタート、レベルアップカードを横並び
- Glitch Survivors 初版：Vampire Survivors 風の 3D 弾幕サバイバー（iPad タッチ操作対応）
